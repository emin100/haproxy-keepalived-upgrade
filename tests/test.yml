---
- hosts: db
  remote_user: root
  roles:
    - name: trendyol.haproxy-keepalived-upgrade
      vars:
        haproxy: true
        keepalived: true
        haproxy_version: 2.4.4
        keepalived_version: 2.2.4
        tmp_directory: /tmp/haproxy-keepalived-upgrade
